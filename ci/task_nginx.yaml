---
platform: linux

image_resource:
  type: docker-image
  source: { repository: 7imbrook/ci-deploy-tools }

inputs:
  - name: frontend

run:
  dir: frontend/conf
  path: docker
  args:
    - build
    - .
