---
platform: linux

image_resource:
  type: docker-image
  source: { repository: node }

inputs:
  - name: frontend_built

params:
  ACCESS_KEY_ID: ((s3.id))
  SECRET_ACCESS_KEY: ((s3.key))

run:
  dir: frontend_built
  path: npm
  args:
    - run
    - build
